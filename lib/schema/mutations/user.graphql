type Mutation {
  CreateUser(
    username: String!,
    password: String!,
    email: String!
  ): User,
  UpdateUser (
    username: String!,
    password: String,
    email: String
  ): User @isAuthenticated
}

type Query {
  Users (
    first: Int @isNaturalNumber
    after: String
    last: Int @isNaturalNumber
    before: String
  ): Page @isAuthenticated
  Login (
    username: String
    password: String
  ): Login
}
